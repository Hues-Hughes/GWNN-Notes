PKI (Public Key Infrastructure)
	Root of Trust - Inter
	Root Key lives offline
	CSR - Certificate Signing Request - generate key pair, write key to disk,
	Private Key never leaves subject.
	
Validity goes up, trust goes down (The Tree of Trust)

Dig Signature process
	OG Doc -> Hash Doc -> Hash Value -> Sign Hash -> Digital Signature

Cryptographic Key Lifecycle - 
	Create(Generation) -> Send(Distribution) - Store(Storage) -> Use key(Usage) -> Replace(Rotation) -> Disable(Revocation) -> Expiration -> Destruction

**Digital Signatures**
	type of electronic signature that uses a specific type of encryption to ensure the authenticity and integrity of a digital message or document. It combines hashing with public key encryption
	
Decentralized vs Centralized Key Management

